---
type: Census Variable
title: '% SELECTED MONTHLY OWNER COSTS AS A PERCENTAGE OF HOUSEHOLD INCOME (SMOCAPI)
  — Housing unit without a mortgage (excluding units where SMOCAPI cannot be computed)
  — Not computed — US Census ACS'
description: This measure indicates the percentage of household income that could
  not be computed for selected monthly owner costs for housing units without a mortgage.
  It pertains to geographic areas such as states, counties, cities, or places. This
  measure focuses on instances where the calculation is not applicable, distinguishing
  it from other measures that report specific percentage ranges. The data is reported
  as a percentage.
tags:
- census
- acs
- profile
- demographics
- community
- needs-assessment
source: ./_access.md
dataset: profile
get: NAME,DP04_0125PE
key: env:CENSUS_API_KEY
variable: DP04_0125PE
representativeQueries:
- What is the situation for homes without a mortgage when SMOCAPI is not computed?
- Can you provide details on the cases where monthly owner costs as a percentage of
  income are not computed for units without a mortgage?
- What does it indicate when the SMOCAPI is not computed for housing units without
  a mortgage?
---

# Schema

ACS 5-year Profile variable `DP04_0125PE` — % SELECTED MONTHLY OWNER COSTS AS A PERCENTAGE OF HOUSEHOLD INCOME (SMOCAPI) — Housing unit without a mortgage (excluding units where SMOCAPI cannot be computed) — Not computed. Returns the value for the requested geography (`geo`); `get`/`key` pinned. See [Census access](./_access.md).
