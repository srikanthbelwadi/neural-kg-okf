numpy>=1.24
pyyaml>=6.0
openai>=1.40          # the one SDK for Azure OpenAI, OpenAI, Gemini (OpenAI-compat), and local (Ollama)
starlette>=0.40       # async Agent Finder and Neural KG ASGI applications
fastapi>=0.115.0
pydantic>=2.8.0
uvicorn>=0.30         # single-worker event-loop server; scale out with instances, not threads
httpx>=0.27           # shared async service and publisher HTTP clients
psycopg[binary]>=3.1  # IRS grant graph when GRANTS_URL / DATABASE_URL selects Postgres
psycopg-pool>=3.2     # application-owned async grant-query pool
google-cloud-bigquery>=3.25.0
google-cloud-aiplatform>=1.60.0
google-auth>=2.30.0
