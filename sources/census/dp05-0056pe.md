---
type: Census Variable
title: '% RACE — Total population — One race — Native Hawaiian and Other Pacific Islander
  — Other Native Hawaiian and Other Pacific Islander — US Census ACS'
description: This variable reports the percentage of the total population identifying
  as 'One race' specifically as 'Other Native Hawaiian and Other Pacific Islander'
  within a defined geographic area. It focuses on a specific racial category, distinguishing
  it from broader racial classifications that include multiple races. The measure
  is expressed as a percentage, indicating the proportion of this specific racial
  group.
tags:
- census
- acs
- profile
- demographics
- community
- needs-assessment
source: ./_access.md
dataset: profile
get: NAME,DP05_0056PE
key: env:CENSUS_API_KEY
variable: DP05_0056PE
representativeQueries:
- What percentage of the population identifies as Other Native Hawaiian and Other
  Pacific Islander?
- How many people in the total population are classified as Native Hawaiian and Other
  Pacific Islander?
- What is the racial composition for Native Hawaiian and Other Pacific Islander individuals?
---

# Schema

ACS 5-year Profile variable `DP05_0056PE` — % RACE — Total population — One race — Native Hawaiian and Other Pacific Islander — Other Native Hawaiian and Other Pacific Islander. Returns the value for the requested geography (`geo`); `get`/`key` pinned. See [Census access](./_access.md).
